# Build via Dockerfile (full control of install + build environment).
# Railpack/Nixpacks couldn't reconcile the Windows-generated lockfile
# with Linux-only optional natives (@emnapi/core, @emnapi/runtime), so
# we own the install step ourselves.

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
